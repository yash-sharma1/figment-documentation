import ReactMarkdown from "react-markdown";

<details open={props.accordionOpen ? 'open' : undefined}>
<summary>
<span className="open">Specification</span>
<span className="closed">View Specification</span>
</summary>

<ReactMarkdown>{props.details}</ReactMarkdown>

</details>
