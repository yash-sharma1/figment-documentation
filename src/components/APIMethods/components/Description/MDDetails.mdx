import ReactMarkdown from "react-markdown";

<details open={props.accordionOpen ? 'open' : undefined}>
<summary>
<span class="open">Specification</span>
<span class="closed">View Specification</span>
</summary>

<ReactMarkdown>{props.details}</ReactMarkdown>

</details>
